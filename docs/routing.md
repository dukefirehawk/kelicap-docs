
# Routing (`kelicap_router`)

- **9.1 Setting up Routing**
  - Declaring `routerDirectives` and `<router-outlet>`.
  - Configuring `RoutePath` and `RouteDefinition`.
- **9.2 Navigating Between Routes**
  - Declarative navigation with `[routerLink]`.
  - Programmatic navigation via `Router`.
- **9.3 Route Lifecycle & Guards**
  - Controlling access: `CanActivate`, `CanDeactivate`.
  - Component reuse optimization: `CanReuse`.
- **9.4 Route Parameters & Query Strings**
  - Reading path arguments (`parameters`) and URL search params (`queryParameters`).

---
